# requirements.txt
# Replication archive: Quantum Kernel Methods for Carbon Price Forecasting
# Manuscript: "Crisis-Period Trading Resilience and Dynamic MSR Reform: 
#              A Quantum Kernel Approach to EU ETS Carbon Price Forecasting"
# Journal: Energy Policy (Elsevier)
#
# IMPORTANT: Use CPython 3.11 (not PyPy).
# Install: pip install -r requirements.txt
# Tested on: Python 3.11.x, Windows 10/11 and Ubuntu 22.04

# ── Core scientific computing ──────────────────────────────────────────
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scipy>=1.10.0
statsmodels>=0.14.0

# ── Quantum machine learning ───────────────────────────────────────────
pennylane>=0.35.0
pennylane-lightning>=0.35.0   # CPU-optimised PennyLane backend

# ── Classical ML ───────────────────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=1.7.0
lightgbm>=4.0.0

# ── Deep learning ──────────────────────────────────────────────────────
torch>=2.0.0
# torch-cuda (GPU): install separately via https://pytorch.org/get-started/locally/
# e.g.: pip install torch --index-url https://download.pytorch.org/whl/cu118

# ── Signal decomposition (EMD-LSTM) ───────────────────────────────────
EMD-signal>=1.6.0              # PyPI package name for PyEMD

# ── Hyperparameter optimisation ────────────────────────────────────────
optuna>=3.3.0

# ── Interpretability ───────────────────────────────────────────────────
shap>=0.42.0

# ── Output and reporting ───────────────────────────────────────────────
openpyxl>=3.1.0
tabulate>=0.9.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0

# ── Statistical testing ────────────────────────────────────────────────
arch>=6.2.0                    # ARCH/GARCH, heteroskedasticity tests
pingouin>=0.5.3                # Additional statistical tests

# ── Utilities ──────────────────────────────────────────────────────────
joblib>=1.3.0
python-dateutil>=2.8.2
pytz>=2023.3
